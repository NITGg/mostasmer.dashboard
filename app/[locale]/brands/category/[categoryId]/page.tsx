import Brands from '@/components/brands/Brands'

export default function CategoryBrandsPage({ params }: { params: { categoryId: string } }) {
    // return <Brands validTo={''} purchaseCount={0} isActive={false} />
    return "aa"
} 